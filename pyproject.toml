[project]
name = "hvac-voice-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pipecat-ai[cartesia,deepgram,local,openai,silero]>=1.5.0",
    "python-dotenv>=1.2.2",
]
